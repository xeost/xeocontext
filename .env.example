# Directory containing the content (Markdown, OpenAPI, AsyncAPI)
# Defaults to 'content' in the project root if not specified.
# Can be an absolute path or a relative path from the project root.
XEOCONTEXT_CONTENT_DIR=./examples/markdown-openapi-asyncapi

# --- Customization & Deployment ---

# Content Source Configuration
# Options: 'local', 'git'
# XEOCONTEXT_CONTENT_TYPE=local

# Path to local directory (if content_type=local) or Git URL (if content_type=git)
# Examples:
# XEOCONTEXT_CONTENT_SOURCE=../my-content-repo
# XEOCONTEXT_CONTENT_SOURCE=https://github.com/my-user/my-content-repo.git
# XEOCONTEXT_CONTENT_SOURCE=

# XeoContext Version Tag (Upstream)
# The tag of the upstream repository (xeost/xeocontext) to pin this app to.
# XEOCONTEXT_TAG=main

# Deployment Repository URL (Origin)
# Your own repository where this customized app will be pushed.
# This is ONLY used to configure the git remote. No commit or push actions are performed automatically.
# XEOCONTEXT_DEPLOY_REPO=https://github.com/my-user/my-xeocontext-deployment.git