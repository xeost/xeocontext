asyncapi: 3.0.0
info:
  title: Library Sync Events
  version: 1.0.0
  description: Real-time synchronization events for Library and Viewers
channels:
  library/changes:
    address: library/changes
    description: Events related to the Library structure (files added/removed)
    messages:
      libraryChange:
        $ref: './components/messages/LibraryChange.yaml'
  library/viewers:
    address: library/viewers
    description: Events related to file viewing state (sync)
    messages:
      viewerSync:
        $ref: './components/messages/ViewerSync.yaml'
